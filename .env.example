# ── Application ──────────────────────────────────────
# Host port to expose the app on (the container always listens on 3000 internally).
PORT=3000
SESSION_SECRET=change-me-to-a-random-64-char-string

# ── Database ─────────────────────────────────────────
# You MUST set DATABASE_URL to a valid PostgreSQL connection string.
#
#   Option A — External / hosted PostgreSQL (Neon, Supabase, Railway, your own server):
#              Just set DATABASE_URL below and run: docker compose up -d
#
#   Option B — Let Docker Compose run PostgreSQL for you:
#              Set DATABASE_URL to the line below and run:
#              docker compose --profile postgres up -d
#              ↓ uncomment this for Option B:
#              DATABASE_URL=postgresql://backslash:backslash@postgres:5432/backslash
#
DATABASE_URL=postgresql://user:password@your-host:5432/backslash

# ── Compilation ──────────────────────────────────────
MAX_CONCURRENT_BUILDS=5
COMPILE_TIMEOUT=120
COMPILE_MEMORY=1g
COMPILE_CPUS=1.5

# ── Auth ─────────────────────────────────────────────
DISABLE_SIGNUP=false
